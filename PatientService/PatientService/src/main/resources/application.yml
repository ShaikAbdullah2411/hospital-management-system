server:
  port: 8083
spring:
  application:
    name: PATIENTSERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/patient_db
    username: root
    password: iramabdullah@312

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
